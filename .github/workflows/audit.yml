name: Docs Audit

on:
  push:
    paths:
      - 'templates/**'
      - 'docs/**'
      - 'methodology/**'
  pull_request:
    paths:
      - 'templates/**'
      - 'docs/**'
      - 'methodology/**'

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check naming convention
        run: python scripts/check-naming.py templates/
      - name: Run basic audit (命名/断链/骨架)
        run: python scripts/basic-audit.py templates/
