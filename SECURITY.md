# 安全策略

## 报告漏洞

如果你发现了安全漏洞，请不要在公开 Issue 中报告。

请发送邮件至项目维护者，或在 GitHub Security Advisory 中提交。

## agentprecept 自身安全

agentprecept 是一个方法论和模板仓库，不包含运行时代码。所有 Python 示例（`examples/todo-api`）仅用于演示。

## 依赖安全

本项目的方法论文档建议用户在 CI 中加入安全扫描。agentprecept 自身不引入外部运行时依赖。
